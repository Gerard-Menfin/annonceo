{% extends 'formulaire.html.twig' %}


{% block form_titre %} Ajouter une catégorie {% endblock %}
{% block form %}
    <section class="section has-background-white-ter">
        {{ form_start(form) }}
            {{ form_row(form.titre) }}
            {{ form_row(form.motscles) }}
            {{ form_row(form.ajouter, { "attr": {"class" : "button is-primary is-fullwidth"} })}}
        {{ form_end(form)}}
    </section>
{% endblock %}
